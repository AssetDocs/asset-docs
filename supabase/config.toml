project_id = "leotcbfpqiekgkgumecn"

[functions.log-consent]
verify_jwt = false

[functions.create-gift-checkout]
verify_jwt = false

[functions.finalize-checkout]
verify_jwt = false

[functions.mint-magic-link]
verify_jwt = false

[functions.resend-magic-link]
verify_jwt = false

[functions.force-signout]
verify_jwt = false

# Supabase Auth "Send Email" hook — called by Auth without a user JWT.
# Authenticates internally via Standard Webhooks signature (SEND_EMAIL_HOOK_SECRET).
[functions.send-auth-email]
verify_jwt = false

# Public — caller proves identity via the raw invite token (delivered to the
# invitee's mailbox), so a user JWT is not required.
[functions.confirm-invite-email]
verify_jwt = false

# Public — caller proves identity via a one-time token from the email-change
# confirmation email sent to the new mailbox. No user JWT required.
[functions.confirm-email-change]
verify_jwt = false

# Internal cron-invoked sweeper — authenticates via x-internal-secret header
# (SUPABASE_SERVICE_ROLE_KEY). No user JWT is presented by pg_cron.
[functions.check-gift-deliveries]
verify_jwt = false

[functions.process-storage-deletion-jobs]
verify_jwt = false

# Internal cron-invoked sweeper — authenticates via x-internal-secret header.
[functions.process-retention-expirations]
verify_jwt = false

# Internal cron-invoked sweeper — authenticates via x-internal-secret header
# AND Authorization: Bearer service-role (used to invoke delete-account).
[functions.process-account-closures]
verify_jwt = false

# Internal cron-invoked sweeper — authenticates via x-internal-secret header.
[functions.process-storage-usage-drift]
verify_jwt = false

# Internal cron-invoked sweeper — authenticates via x-internal-secret header.
# Conservative: only seeds storage_orphan_candidates; never deletes inline.
[functions.process-storage-orphans]
verify_jwt = false

# Internal cron-invoked sweeper - authenticates via x-internal-secret header.
# Expires continuity export authorizations and removes stale export bundles.
[functions.process-expired-exports]
verify_jwt = false

# Internal cron-invoked reminder - authenticates via x-internal-secret header.
# Sends only when no passed restore drill is recorded in the due window.
[functions.quarterly-restore-drill-reminder]
verify_jwt = false

# Internal cron-invoked sweeper - authenticates via x-internal-secret header.
# Redacts old closed support issue free-text PII after the retention window.
[functions.scrub-old-support-pii]
verify_jwt = false

# Public webhook receiver — Resend calls without a user JWT.
# Authenticates via Svix signature (RESEND_WEBHOOK_SECRET) once configured.
[functions.resend-webhook]
verify_jwt = false
