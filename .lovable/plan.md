
6 targeted changes across 4 files. All are simple text/link edits.

## Files & Changes

**1. `src/components/Footer.tsx` — line 186**
- Change `to="/features#scenarios"` → `to="/scenarios"`

**2. `src/components/DocumentProtectSection.tsx` — lines 147–154**
- Remove the entire `<div className="flex flex-col sm:flex-row gap-4 justify-center">` block containing both the "Features & Tools" and "Solutions" buttons (both CTAs removed)

**3. `src/components/ScrollProgressCue.tsx`**
- Change `"You're halfway through — here's why Asset Safe is different."` → `"What makes Asset Safe different"`

**4. `src/components/HeroSection.tsx`**
- Change `"View Sample Dashboard"` → `"See how it works"`
- Change `"Start Documenting"` → `"Get started"`

That's all 6 changes across 4 files. No logic changes needed.
